services:
  - type: web
    name: merchant-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m app.main_dev
    healthCheckPath: /health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: LOG_LEVEL
        value: INFO
